{% extends 'base.html.twig' %}

{% block title %}
	{% if edit is defined %}
		Editar Podcast
	{% else %}
		Registrar podcast
	{% endif %}

{% endblock %}

{% block body %}
	<div class="container h-100 ">
		<div class="row h-100 justify-content-center align-items-center ">
			<div class="col-10 bg-white rounded my-3 p-5">
				<h1>{% if edit is defined %}
						Editar Podcast
					{% else %}
						Registrar podcast
					{% endif %}</h1>
				{{ form(form) }}
				</div>
			</div>
		</div>

	{% endblock %}
